<<<<<<< HEAD
services:
  postgis-eq:
    image: postgis/postgis:16-3.4
    container_name: postgis-eq
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: gis
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - pgnet
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  api-eq:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: api-eq
    restart: unless-stopped
    ports:
      - "3002:3002"
    depends_on:
      postgis-eq:
        condition: service_healthy
    networks:
      - pgnet

  martin-eq:
    image: ghcr.io/maplibre/martin:latest
    container_name: martin-eq
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      RUST_LOG: info
      DATABASE_URL: "postgresql://postgres:postgres@postgis-eq:5432/gis"
    command: ["--auto-publish", "from-schemas=earthquake"]
    depends_on:
      postgis-eq:
        condition: service_healthy
    networks:
      - pgnet

volumes:
  pgdata:

networks:
  pgnet:
    driver: bridge
=======
services:
  postgis:
    image: postgis/postgis:16-3.4
    ports:
      - "15432:5432"
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./sql:/docker-entrypoint-initdb.d:ro
    networks:
      - eq-viewer-net
    # This healthcheck ensures the database is fully ready before other services start.
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d gis"]
      interval: 5s
      timeout: 5s
      retries: 5

  martin:
    image: ghcr.io/maplibre/martin:latest
    depends_on:
      # This tells Martin to wait until the postgis service is healthy.
      postgis:
        condition: service_healthy
    ports:
      - "${MARTIN_PORT}:3000"
    environment:
      DATABASE_URL: "postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgis/${POSTGRES_DB}"
      MARTIN_SCHEMAS: landslides,public
    command: ["--webui", "enable-for-all"]
    networks:
      - eq-viewer-net

  web:
    image: nginx:alpine
    depends_on: [martin]
    ports:
      - "8081:80"
    volumes:
      - ./web:/usr/share/nginx/html:ro
    networks:
      - eq-viewer-net

networks:
  eq-viewer-net:

volumes:
  pgdata:
>>>>>>> 7fe243ab3935f781518ab69caf6a5cf9d625a67d
